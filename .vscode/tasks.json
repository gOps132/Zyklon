{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format

    // Build with cmake and run the executable in the ${worspaceDirectory}
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Sandbox-Debug",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "command": "./bin/Sandbox-Debug/sandbox",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}